<?xml version="1.0" encoding="utf-8"?>
<resources>

    <attr name="color_custom" format="color" />
    <attr name="color_move" format="color" />
    <attr name="color_error" format="color" />
    <attr name="line_custom_size" format="float" />
    <attr name="line_move_size" format="float" />
    <attr name="count" format="integer"/>
    <attr name="tryTimes" format="integer" />


    <attr name="dotSize" format="integer"></attr>
    <attr name="normal_color" format="color"></attr>
    <attr name="action_color" format="color"></attr>




    <declare-styleable name="GestureLockViewGroup">
        <attr name="color_custom" />
        <attr name="color_move" />
        <attr name="color_error" />
        <attr name="line_custom_size"/>
        <attr name="line_move_size"/>
        <attr name="count"  />
        <attr name="tryTimes" />
    </declare-styleable>


    <declare-styleable name="DotViewGroup">
        <attr name="count"/>
        <attr name="dotSize"/>
        <attr name="normal_color"/>
        <attr name="action_color"/>

    </declare-styleable>

    <declare-styleable name="ToggleView">
        <attr name="switch_background" format="reference" />
        <attr name="slide_button" format="reference" />
        <attr name="switch_state" format="boolean" />
    </declare-styleable>

    <declare-styleable name="SecurityCenter">
        <!-- 选中状态的手势点-->
        <attr name="selectedBitmap" format="reference"/>
        <!-- 未选中状态的手势点-->
        <attr name="unselectedBitmap" format="reference"/>
        <!-- 选中状态的手势小点-->
        <attr name="selectedBitmapSmall" format="reference"/>
        <!-- 未选中状态的手势小点-->
        <attr name="unselectedBitmapSmall" format="reference"/>
        <!-- 验证失败后再次验证的拦截时间-->
        <attr name="waitTime" format="integer"/>
        <!-- 验证的最大失败次数-->
        <attr name="maxFailCounts" format="integer"/>
        <!-- 绘制时最少连接的点数-->
        <attr name="minPoint" format="integer"/>
        <!-- 字体的颜色-->
        <attr name="paintColor" format="color"/>
        <!-- 字体的大小-->
        <attr name="paintTextSize" format="dimension"/>
    </declare-styleable>
</resources>